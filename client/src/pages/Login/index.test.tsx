import React from "react";
import { describe, test } from "vitest";

import Login from ".";

describe("Login Test", () => {
  test("should display a blank login form, with remember me checked by default", async () => {
    // here we render the login component
  });
});