import { describe, test, expect } from 'vitest';
import Register from ".";

describe("Register Test", () => {
  test("should be able to register", () => {
    
    // // create variables for the elements we want to test
    // const loginInput = {
    // "username": "testuser",
    // "login": "testlogin",
    // "password": "testpassword"
    // };

    // // fill in the registration form
    // fireEvent.change(screen.getByTestId("login:"), { target: loginInput["login"] });
    // fireEvent.change(screen.getByTestId("username:"), { target: loginInput["username"] });
    // fireEvent.change(screen.getByTestId("password:"), { target: loginInput["password"] });
    // fireEvent.click(screen.getByTestId("submit"));

    // // check if the user was redirected to the login page
    // expect(screen.getByText("Projects")).toBeInTheDocument();
  });
});