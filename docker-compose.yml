version: "3.8"

services:
  mysqldb:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: "ppp"
      MYSQL_ALLOW_EMPTY_PASSWORD: true

  app:
    depends_on:
      - mysqldb
    build: ./serverORM/
    ports:
      - 5000:5000