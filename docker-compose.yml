version: '3'

services:
  mysqldb:
    build: ./db/
    restart: always
    ports:
      - 3306:3306

  app:
    depends_on:
      - mysqldb
    build: ./server/
    restart: always
    ports:
      - 5000:5000