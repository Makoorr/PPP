version: "3.8"
networks:
  app-tier:
    driver: bridge

services:
  app:
    build: 
      context: ./serverORM/
      dockerfile: Dockerfile
    ports:
      - 5000:5000
    networks:
      - app-tier

  client:
    depends_on:
      - app
    build: 
      context: ./client/
      dockerfile: Dockerfile
    ports:
      - 80:80
    networks:
      - app-tier